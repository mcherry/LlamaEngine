import SwiftUI

/// LlamaEngineUI — reusable SwiftUI screens and rendering views.
///
/// Placeholder so the target compiles in Phase 0; the real reusable views
/// (`MarkdownView`, `MermaidView`, `MessageRow`, `Composer`, a `ChatScene`, …) arrive
/// in Phase 4.
public struct LlamaEnginePlaceholderView: View {
    public init() {}

    public var body: some View {
        Text("LlamaEngineUI")
    }
}
